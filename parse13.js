// when we receive data from web It is in string formate
// and we parse the data into JavaScript Object with below method
// JSON.parse(data)

// Example 
// var obj=JSON.parse('{"name":"anwaar","age":36}')

// Excepption 
// Date Object and function are not allowed in JSON
// if you include a date or a function write it in string
// YOu can convert it back into a date object and or function later


